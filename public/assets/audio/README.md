# SHUXIN event audio

Four supplied MP3 files are loaded by Vite from `assets/audio/`. Off-route answers reuse `resonance-up.mp3` at 30% of the configured event volume. The application deliberately provides no downloaded or generated fallback audio.

Background ambience is loaded by Vite from `assets/audio/shuxin-ambience.mp3`.
